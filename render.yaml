services:
  - type: web
    name: autofulfill-ai
    env: node
    plan: free # or starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000 # Render assigns a port, but setting a default is good practice
